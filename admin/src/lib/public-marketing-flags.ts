/**
 * Temporary toggles for public marketing chrome.
 * Flip to `true` when you want these sections visible again.
 */
/** Entire home hero (`hero-v3`): mockup + preview image. Off → page starts at «Гишүүн байгууллагууд». */
export const SHOW_HOME_HERO_V3_SECTION = false;

/** Hero left: headline, subcopy, trip/event buttons, weekly blurb, «7 хоногийн хурал», «Хэрхэн ажилладаг вэ?» */
export const SHOW_HOME_HERO_LEFT_MARKETING_SECTION = false;

export const SHOW_HOME_HERO_CREATE_TRIP_AND_EVENT_BUTTONS = false;
export const SHOW_PUBLIC_HEADER_LOGIN_REGISTER = false;

/** Public navbar links — off hides the item; definitions stay in `Navbar` / `SiteHeaderNav` for later. */
export const SHOW_PUBLIC_NAV_COMPANIES = false;
export const SHOW_PUBLIC_NAV_INVESTMENTS = false;
export const SHOW_PUBLIC_NAV_MEMBERS = false;
export const SHOW_PUBLIC_NAV_BUSY_AI = false;
